# Testing Strategy

- Currently, there is **no formal testing framework** configured.
- Automated tests (Jest/PyTest) are missing from the mapped files.
- Testing relies on manual Verification through the frontend UI and DB queue state.
